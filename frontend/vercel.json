{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm run build",
  "regions": ["fra1"],
  "git": {
    "ignoredBuildStep": "[ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ] || git diff --quiet HEAD^ HEAD -- ."
  }
}